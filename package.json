{
  "name": "react-native-animated-expandable-view",
  "version": "1.0.0",
  "description": "Simple expandable view.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.tsx\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "react-native",
    "collapsed",
    "expandable"
  ],
  "author": "Lukasz Kurant",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/caki0915/my-awesome-greeter.git"
  },
  "devDependencies": {
    "@types/react": "^16.9.49",
    "@types/react-native": "^0.63.18",
    "prettier": "^2.1.1",
    "react": "^16.13.1",
    "react-native": "^0.63.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.2"
  }
}
